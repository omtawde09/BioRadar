# Copy to .env and adjust. .env is gitignored.
POSTGRES_USER=bioradar
POSTGRES_PASSWORD=bioradar
POSTGRES_DB=bioradar
DB_PORT=5432

BACKEND_PORT=8000
DASHBOARD_PORT=3000

# Required before starting the backend. Generate one:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=

BIORADAR_PIPELINE_IMAGE=ghcr.io/omtawde09/bioradar-pipeline:v1.0
PIPELINE_CORES=4
